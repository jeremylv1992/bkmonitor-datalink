# AGENTS.md

本文件记录本仓库内协作代理的工作约定。优先遵守用户的直接指令；若与本文件冲突，以用户最新指令为准。

## 项目上下文

- 当前仓库是 `bkmonitor-datalink`，包含多个 Go module，常见工作目录在 `pkg/` 下。
- 运行 Go 测试前先确认目标模块的 `go.mod` 所在目录。例如 `unify-query` 相关测试应在 `pkg/unify-query` 下执行。
- 外部关联仓库可能位于同级目录，例如 `bk-monitor-gf`。修改该仓库前先确认当前分支，并避免污染 release 分支。

## 本地信息来源

- 在需要了解个人习惯、本地验证方式、线上验证方式、测试环境、跳板机、服务部署位置或临时脚本时，先尝试查看 `.local/` 目录下的说明文件。
- `.local/` 是本地私有信息目录，可能包含账号、地址、令牌或环境细节。不得把其中的敏感内容复制到可提交文件、最终回复或公开日志中。
- 如果 `.local/` 中的信息与仓库文档冲突，先按 `.local/` 中的本地约定执行，并在回复中只概括所采用的方法，不暴露敏感值。

## 开发约定

- 搜索文件或文本优先使用 `rg` / `rg --files`。
- 手工修改文件优先使用 `apply_patch`，避免用脚本做不必要的大范围重写。
- 保持改动聚焦，不重构无关代码，不回滚用户已有改动。
- 新增逻辑应尽量沿用现有包结构、命名和测试风格。

## 验证约定

- 修改后运行与改动面直接相关的目标测试，并在最终回复中说明实际执行过的命令和结果。
- 若全量测试成本高或存在已知无关失败，优先跑目标测试，并明确说明未跑全量测试。
- 需要线上或本地集成验证时，先查看 `.local/` 中是否有对应环境、命令或访问说明。

## Git 约定

- 开发分支默认使用 `codex/` 前缀，除非用户指定其他分支名。
- 不执行 `git reset --hard`、`git checkout --` 等破坏性操作，除非用户明确要求。
- 提交前先检查 `git status --short --branch`，确认未混入无关文件。
